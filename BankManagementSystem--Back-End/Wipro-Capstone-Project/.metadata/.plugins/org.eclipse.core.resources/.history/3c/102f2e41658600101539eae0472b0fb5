package com.bank.payment.controller;

import lombok.extern.slf4j.Slf4j;
import org.springframework.web.bind.annotation.*;

import com.bank.notification.dto.PaymentRequest;
import com.bank.notification.entity.Payment;
import com.bank.notification.service.PaymentService;

@Slf4j
@RestController
@RequestMapping("/payments")
public class PaymentController {

	private final PaymentService service;

    public PaymentController(PaymentService service) {
        this.service = service;
    }

    @PostMapping("/transfer")
    public Payment doPayment(@RequestBody PaymentRequest request) {
        return service.doPayment(request);
    }
}
